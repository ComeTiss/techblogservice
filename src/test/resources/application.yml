spring:
  jpa:
    hibernate:
      dialect: "org.hibernate.dialect.H2Dialect"
      ddl-auto: "create-drop"

  datasource:
    driverClassName: "org.h2.Driver"
    url: "jdbc:h2:mem:blogTest;DB_CLOSE_ON_EXIT=FALSE"
    username: "userTest"
    password: ""
